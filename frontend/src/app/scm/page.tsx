import ProductCatalog from '@/components/scm/ProductCatalog';

export const metadata = {
    title: 'SCM Demand Intelligence | LifeShield',
    description: 'Real-time regional demand analysis for emergency preparedness assets.',
};

export default function SCMPage() {
    return <ProductCatalog />;
}
